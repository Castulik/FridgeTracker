<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- hlavní motiv celé aplikace -->
    <style name="Theme.FridgeTracker_001" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- ↓ sem si klidně nech defaultní barvy nebo přidej vlastní -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorSecondary">@color/purple_200</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/white</item>
        <!-- pokud nepoužíváš vlastní barvy, klidně tyto <item> smaž -->
    </style>

</resources>
